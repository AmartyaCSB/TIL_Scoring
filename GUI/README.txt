Example of how our GUI is implimented. /Images contain the pre-computed image files, in the final version these will be computed during load and not saved separately.

To use:

variable to change: under the if name == main: change "path" to the system path pointing to the folder containing GUI.py